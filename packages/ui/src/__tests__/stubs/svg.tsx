/** react-native-svg stub — dekho stubs/icons.tsx */
import { createElement } from 'react';

const Svg = () => createElement('span', { 'data-svg': true });

export const Path = Svg;
export const Circle = Svg;
export const G = Svg;
export default Svg;
